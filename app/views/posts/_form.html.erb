<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
  <% post.errors.full_messages.each do |message| %>
    <li><%= message %></li>
      <% end %>
    </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body%>
    <%= form.text_area :body, :cols => "40", :rows => "20" %>
  </div>

  <div class="field">
    <%= form.label :Monthly_Price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.file_field :post_pics, multiple: true %>
  </div> 

  <div class="field">
    <%= form.label :Are_you_a_driver %>
    <%= form.check_box :car_owner %>
  </div> 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

